# WWMM (Wuthering Waves Mod Manager) – 리팩토링 & 설계 정리 초안

## ✅ 1. 프로젝트 목적

- 단순한 모드 설치 도구가 아니라, **사용자 친화적이고 감각적인 모드 런처**로 발전시키는 것이 목표.
- 현재 프로그램 UI를 완전히 갈아엎는 것이 아니라, **스타일·분위기만 차용한 리팩토링**을 원함 (예: 원신 런처 스타일).
- “저 UI 그대로 복붙”이 아니라, **디자인 방향・배치・분위기만 참고**하는 것이 중요.

---

## ✅ 2. 현재 프로그램 상태 (요약)

| 요소 | 설명 |
|------|------|
| UI 구조 | PyQt5 기반. 좌측 캐릭터 선택 / 우측 모드 목록 / 버튼 형태의 관리자창 |
| 기능 | - 모드 추가 (`shutil.copytree`)<br>- 모드 삭제 (`rmtree`)<br>- preview 이미지 표시<br>- 관리자 권한 체크 및 자동 재실행 |
| 파일 구조 | `Mods/<캐릭터명>/<모드명>/` 내부에 preview 이미지 존재 |
| 권한 처리 | `ctypes.windll.shell32.IsUserAnAdmin()` 으로 UAC 확인 및 재실행 가능 |
| 문제점 | UI가 일반적인 툴 느낌, “런처” 스타일 감성 부족. 구조적으로 확장성 떨어짐. |

---

## ✅ 3. 리팩토링 목표 (디자인 스타일)

### 🎨 **디자인 방향**
- 다크톤 / 유리광택 느낌 / 최소한의 버튼 / 주요 버튼만 강조  
- 배경 = 큰 일러스트 + 얇은 어두운 레이어  
- 상단 바에 아이콘(Discord, GitHub, 설정 등)  
- 중앙에 큰 `Start / Launch` 버튼 배치  
- 좌측 사이드바는 최소화하거나 상단 드롭다운 방식으로 변경  
- **UI 복제(X), 스타일 참고(O)** — 이것이 핵심

---

## ✅ 4. 리팩토링 후 예상 화면 구성

### 📌 **1) 홈 화면 (Launcher 스타일)**
- 상단바 (로고 / 설정 / GitHub 등)
- 배경 이미지 + 딤(dimming)
- 중앙: **Start 버튼 + 추가 옵션(...)**
- 좌하단: 버전 정보 (CORE / XXMI / WWMM 등)

### 📌 **2) 모드 관리 화면**
- 좌측: 캐릭터 선택 (아이콘 또는 드롭다운)
- 중앙: 모드 카드(Grid 형태, preview + on/off 토글)
- 우측: 상세 정보 (미리보기 큰 이미지, 설명, 폴더 경로 등)

### 📌 **3) 설정 화면**
- 모드 경로 / 게임 실행 파일 경로
- 백업 / 캐시 / 관리자 권한 관련 옵션
- (선택) 오버레이 기능 켜기/끄기

---

## ✅ 5. 추가하고 싶은 기능 목록

| 기능 | 설명 | 미리 가능성 판단 |
|------|------|------------------|
| 게임 실행 전 런처 스타일 UI | 모드 선택 → 게임 실행 흐름 | ✅ |
| 오버레이 UI (게임 내 표시) | 게임 화면 위에 UI 띄우기 | ⚠ 기술적으로 가능하지만 방식에 따라 법적/안티치트 이슈 고려 |
| 게임 내에서 모드 ON/OFF | 실시간으로 모드 바꾸고 눈으로 확인 | ⚠ 일부 모드만 가능 (텍스처/셰이더 등), 대부분은 재시작 필요 |
| 모드 프로필 기능 | 캐릭터+모드 조합 저장 후 실행 | ✅ |
| 관리자 권한 자동 요청 | 부족한 경우 자동 재실행 | ✅ |
| UI 테마 시스템 | 다크/라이트 테마 또는 계절별 테마 | 옵션 |

---

## ✅ 6. 게임 내부 오버레이 고려사항

| 방법 | 설명 | 장점 | 단점 |
|------|------|------|------|
| 투명 오버레이 창 | 바탕화면 최상단에 띄움 | 안전 | 전체화면 게임에서는 한계 |
| ReShade + ImGui Addon | 이미 존재하는 렌더 후킹 활용 | 합법적 가능성 높음 | ReShade 설치 필요 |
| DirectX Hook 직접 구현 | 게임 내부에 UI 삽입 | 자유도 높음 | 안티치트/법적 리스크 커서 비추천 |

---

## ✅ 7. 메모 작성/관리 방법 제안

### 폴더 예시:

WWMM/
├─ wwmm_start.py                  # 프로그램 실행 (EXE 빌드 시 진입 파일)
│
├─ assets/
│   └─ placeholder.png            # 기본 이미지/리소스
│
├─ core/                          # UI와 분리된 실제 기능/로직 영역
│   ├─ character_manager.py       # (예정) 캐릭터 정보/순서 등 관리
│   ├─ directory_link_manager.py  # 정션(Junction) 생성/삭제 (mklink /J 기반)
│   ├─ mod_manager.py             # 모드 추가/삭제/프리뷰 이미지 관련 기능
│   ├─ settings_manager.py        # settings.json 저장/로드 관리
│   └─ system_manager.py          # 관리자 권한 확인 및 run_as_admin 기능
│
├─ resources/                     # 외부 파일/설정/리소스 저장소
│   ├─ config/                    # (예정) JSON 등 설정 파일
│   ├─ font/                      # 폰트 파일 (예: AstaSans-Regular.ttf)
│   ├─ icons/                     # 캐릭터/속성 아이콘
│   └─ mods/                      # WWMM에서 관리하는 모드 원본 저장 위치
│
└─ ui/
    ├─ views/
    │   ├─ main_window.py         # 메인 윈도우 (WWMM UI의 전체 구조)
    │   └─ mods_view.py           # 모드 목록 표시/갱신 영역
    │
    ├─ widgets/
    │   ├─ drop_container.py      # 드래그 앤 드롭으로 모드 추가하는 위젯
    │   ├─ mod_cards.py           # 개별 모드 카드 UI 위젯
    │   └─ preview_label.py       # 미리보기 이미지 처리/드래그 업데이트 위젯
    └─ wwmm_logic_bridge.py       # 기존 전체 UI가 모여 있는 파일 (분리 진행 중)

--------------------------------------------------------------------------------------------------------------------------

### 📌 wwmm_mod_utils.py (→ core/mods.py 예정)

**역할**  
- 모드 폴더 복사/삭제  
- 관리자 권한 여부 확인  
- preview 이미지 탐색  
- 단, 현재는 PyQt5 메시지 박스를 직접 호출 ⇒ UI와 로직이 강하게 결합됨 (리팩토링 필요)

**주요 함수**
| 함수 | 설명 |
|------|------|
| is_admin() | 현재 프로세스가 관리자 권한인지 반환 |
| run_as_admin(exe_path, params) | 관리자 권한으로 프로그램 재실행 |
| add_mod_folder() | 캐릭터 폴더에 모드 복사. 완료 후 UI 갱신 콜백 실행 |
| delete_mod() | 기존 모드 삭제. 완료 후 UI 갱신 콜백 실행 |
| get_preview_image_path() | 폴더 내 이미지 파일 존재하면 반환 |

**리팩토링 방향**
- UI 메시지 제거 → 예외/반환값 중심 구조  
- `core/mods.py`, `core/admin.py` 등에 기능별 분리  
- 경로는 `pathlib` 기반 사용 고려

--------------------------------------------------------------------------------------------------------------------------

### 📌 settings.py (→ core/settings_manager.py 예정)

**역할**  
- 설정 파일(JSON)을 읽고/저장하는 기능  
- 현재 저장되는 항목:  
  - `wwmi_mods_path` — 모드 저장 루트 경로  
  - `character_order` — UI에서 캐릭터 표시 순서  
  - `xxmi_launcher_path` — XXMI 실행 파일 경로

**주의할 점**  
- 설정 파일이 존재하지 않거나 JSON 형식 문제 발생 시 → 기본값 반환  
- GUI 코드와 분리되어 있음 → 구조적으로 좋은 상태  
- 단, 단순 튜플 반환이므로 추후 유지보수 시 불편 → 클래스로 변경 권장

--------------------------------------------------------------------------------------------------------------------------

## 📌 wwmm_symlink_helper.py — 심볼릭 링크 헬퍼(CLI 전용)

### 1) 파일 역할
- 심볼릭 링크 **생성/제거를 별도 프로세스에서 수행**하여 권한·예외를 격리.
- 표준 출력으로 **성공/실패 메시지**를 고정 포맷으로 제공하고, 종료코드로 결과 표시.

---

### 2) 제공 명령 (CLI)

- `apply`: `<dst>`가 이미 존재하면, 링크면 `unlink`, 폴더면 `shutil.rmtree`로 제거 후 `os.symlink(src, dst, target_is_directory=True)`.
- `remove`: `<dst>`가 존재하면 링크/디렉터리 구분해 제거. 없으면 OK로 종료.

**출력 규칙**
- 성공: `OK` 출력 후 exit 0
- 실패: `ERR:<메시지>` 출력 후 exit 1
- 인자 오류: `ERR:인자 부족`, `ERR:apply 인자 부족`, `ERR:remove 인자 부족`, `ERR:알 수 없는 명령`

---

### 3) 내부 동작 요약
- 모든 경로는 `os.path.abspath`로 정규화.
- 기존 대상이 **링크인지 디렉터리인지** 구분하여 처리.
- 예외 발생 시 즉시 에러 메시지와 함께 종료.

--------------------------------------------------------------------------------------------------------------------------

## 📄 wwmm_symlink.py — Windows 호환 심볼릭 링크 생성

### 1) 파일 역할
- `create_symlink_windows_compatible(src, dst, parent_widget=None)`
  - 원본 경로(`src`)와 대상 경로(`dst`)를 받아, **Windows에서 디렉터리용 심볼릭 링크**를 생성합니다.
  - 대상 경로가 이미 있으면 제거 후(`unlink` 또는 `rmtree`) `mklink /D`로 링크를 만듭니다.
  - 오류/경고는 `QMessageBox`로 바로 표시합니다.

---

### 2) 동작 흐름
1. `src`/`dst`를 절대경로로 정규화.
2. `src` 존재 확인. 없으면 경고 후 반환.
3. `dst` 상위 디렉터리 생성.
4. `dst`가 존재하면 링크 여부에 따라 `os.unlink` 또는 `shutil.rmtree`로 제거.
5. `cmd /c mklink /D "dst" "src"`를 `subprocess.call`로 실행.

---

### 3) 눈에 띄는 이슈
- **`shutil` 미import**: 코드에서 `shutil.rmtree`를 쓰지만 모듈을 import하지 않았습니다.
- **반환값/성공 판정 부재**: `subprocess.call`의 반환 코드 확인이 없어, 실패해도 성공처럼 보일 수 있습니다.
- **UI와 로직 결합**: 내부에서 `QMessageBox`를 직접 호출합니다. 비즈니스 로직과 UI 분리가 필요합니다.
- **Windows 전용 명령 의존**: Python의 `os.symlink`를 먼저 시도하고, 실패 시 `mklink`로 폴백하는 전략이 더 이식성 있습니다.
- **권한 이슈 고려 부족**: `mklink`는 보통 관리자 권한 또는 개발자 모드가 필요합니다. 실패 원인별 안내가 필요합니다.
- **링크 유형 판단 한계**: 기존 대상이 **정상 폴더(정규 디렉터리)** 인 경우 `os.path.islink(dst)`가 `False`라 `rmtree`로 지웁니다. 의도된 동작이 맞는지 확인 필요합니다.
- **예외 처리 방식**: `return`으로 조용히 빠지는 분기가 많아 호출 측에서 상태를 알기 어렵습니다.

--------------------------------------------------------------------------------------------------------------------------

# 📄 wwmm_ui.py (메인 UI & 단일 인스턴스 & 모드 카드 뷰)

## 1) 파일 역할 개요
- **단일 인스턴스 보장**: 홈 디렉터리 `~/.wwmm.lock`(PID 저장)로 중복 실행 방지 (`psutil` 사용).
- **메인 창(UI)**: 프레임리스 창(커스텀 타이틀바) + 좌측 캐릭터 트리 + 우측 모드 카드 그리드 + 상단 경로/실행 컨트롤.
- **드래그&드롭**:
  - 모드 폴더를 드래그하여 추가(컨테이너/빈 카드에 드롭).
  - 미리보기 이미지 파일을 카드로 드롭하여 `preview.(png|jpg|...)` 교체.
- **모드 적용/해제/삭제**:
  - 적용/해제: **관리자 권한**으로 `wwmm_symlink_helper`(apply/remove) 실행 → 1.5s 후 갱신.
  - 삭제: `wwmm_mod_utils.delete_mod` 호출.
- **설정 로드/저장**: `settings.json`(앱 베이스 디렉터리)에서 `wwmi_mods_path`, `character_order`, `xxmi_launcher_path` 입출력.
- **외부 런처 실행**: `Run XXMI Launcher` 버튼 → `run_as_admin`으로 .exe 실행.

---

## 2) 단일 인스턴스 처리
- `LOCK_FILE = ~/.wwmm.lock`에 PID 기록.
- 시작 시:
  - 파일이 있고 PID가 살아있으면 `QMessageBox`로 “이미 실행 중”.
  - 죽은 PID이면 lock 제거 후 실행.
- 종료 시: `atexit.register(remove_lock)`로 잠금 제거.

---

## 3) UI 구성 요소
- **프레임리스 창** + 커스텀 타이틀바(최소화/최대화/닫기, 드래그 이동 지원).
- **상단 바**: WWMI 경로 라벨, `WWMI Route Setting`, `Run XXMI Launcher` 버튼.
- **좌측 패널**: `QTreeWidget`(카테고리/캐릭터 트리)
  - `config`는 아니고 코드 내부에 **카테고리/캐릭터/아이콘/색상**이 **하드코딩**.
  - 실제 `Mods/<캐릭터명>/` 디렉터리가 존재하는 캐릭터만 트리에 표시.
- **우측 영역**: `QScrollArea` 안에 `ModCardsContainer`
  - 상단에 `＋ 모드 추가` 버튼
  - 아래 **그리드(2열)** 로 모드 카드들
  - 캐릭터 미선택/빈 상태일 땐 `EmptyModDropCard` 안내 표시
- **테마**: 다크 팔레트 & 위젯별 inline 스타일시트(QSS) 사용.

---

## 4) 주요 클래스 & 동작

### `ModCardsContainer(QWidget)`
- 폴더 드래그&드롭 허용 → 현재 선택 캐릭터가 있어야 `add_mod_folder(...)` 호출.

### `EmptyModDropCard(QFrame)`
- 빈 상태 안내 카드. 폴더 드래그&드롭으로 모드 추가.

### `PreviewLabel(QLabel)`
- 이미지 파일 드롭으로 `preview.*` 교체.
- 클릭 시 파일 선택 다이얼로그 열어 교체.

### `WWMM(QWidget)`
- **상태**: 
  - `wwmm_mods_path` (앱/배포 기준), `wwmi_mods_path`(설정으로 선택), `settings_file`
  - `current_character`, `character_order`
- **초기화 흐름**:
  1) 베이스 디렉터리 계산(스크립트/배포 분기), 아이콘/창 속성 지정  
  2) `init_ui()`로 레이아웃 구성  
  3) `load_settings()` → 경로 라벨 반영  
  4) `load_characters()` → 좌측 트리 구성  
  5) `show_empty_card()`  
- **캐릭터 로딩**: 
  - 카테고리/색상/아이콘 **딕셔너리 하드코딩**  
  - `Mods/<캐릭터>` 디렉터리 존재 확인 후만 트리에 추가  
- **모드 카드 생성**: 
  - 각 카드: 제목(적용됨 배지), 미리보기(300×200), 버튼 2개(적용/해제, 삭제)
  - `is_applied` 비교는 `get_applied_mod_name()`(WWMI 대상 링크 검사)로 판단
- **적용/해제**:
  - `toggle_mod(mod_name, is_applied)`
    - **적용**: `call_symlink_helper_as_admin("apply", char_mod_path, wwmi/char)`  
    - **해제**: `call_symlink_helper_as_admin("remove", "", wwmi/char)`
  - 헬퍼는 **관리자 권한**으로 별도 프로세스 실행. 1.5초 후 `on_character_selected`로 새로고침.
- **런처 실행**:
  - `launch_xxmi_launcher()`에서 경로 없으면 설정 유도 후 `run_as_admin(path, [])`로 실행.

---

## 5) 파일/함수 간 의존 관계
- `wwmm_mod_utils`: `add_mod_folder`, `delete_mod`, `get_preview_image_path`, `is_admin`, `run_as_admin`
- `wwmm_symlink_helper`: 관리자 권한으로 apply/remove
- `wwmm_symlink.create_symlink_windows_compatible`: (직접 호출은 현재 없음 — 헬퍼 경로가 메인)
- `wwmm_settings`: `load_settings`, `save_settings`



# WWMM 리팩토링 진행 내용 정리

## 1. 프로젝트 목표

### 기존 구조 문제점
- `wwmm_ui.py` 내부에 UI, 파일 처리, 권한 검사, 로직이 모두 혼합됨.
- 유지보수 어려움 및 확장성 부족.
- 디자인 개선 및 기능 구조화를 위한 리팩토링이 필요.

### 리팩토링 목표
| 목표 | 설명 |
|------|------|
| UI와 로직 분리 | UI는 표시 역할만, 실제 동작은 core 모듈에서 처리 |
| 폴더 구조 명확화 | core / ui / utils 형태로 역할 구분 |
| 관리자 권한 최소화 | symbolic link → junction 방식으로 변경 |
| 미래 확장 고려 | 게임 내 UI 표시, 토글형 띄우기, UI 디자인 스타일 반영 가능 구조 |

---

## 2. 폴더 구조 (현재 리팩토링 방향)

```
wwmm/
 ├─ wwmm_start.py               # 메인 실행 파일 (유지)
 ├─ core/
 │   ├─ mod_manager.py          # 모드 추가/삭제/프리뷰 탐색
 │   ├─ system_manager.py       # 관리자 권한 확인 + run_as_admin
 │   ├─ settings_manager.py     # 설정 저장/로드
 │   ├─ directory_link_manager.py   # (NEW) 정션(Junction) 기반 링크 생성/삭제
 │
 ├─ ui/
 │   └─ wwmm_ui.py              # 기존 UI (점차 분리 예정)
 │
 ├─ settings.json               # 사용자 설정 저장 파일
```

---

## 3. 리팩토링된 파일별 변경 내역

| 기존 파일 | 리팩토링 후 분리 위치 | 분리된 기능 |
|-----------|------------------------|-------------|
| `wwmm_mod_utils.py` | `core/mod_manager.py` | 모드 추가, 삭제, 프리뷰 이미지 탐색 |
| `wwmm_mod_utils.py` | `core/system_manager.py` | `is_admin`, `run_as_admin` 함수 분리 |
| `wwmm_settings.py` | `core/settings_manager.py` | 설정 로드 및 저장 기능 유지 |
| `wwmm_symlink.py` & `wwmm_symlink_helper.py` | `core/directory_link_manager.py` | 심볼릭 링크 제거 → 정션 링크(`mklink /J`) 기반 구현 |
| `wwmm_ui.py` | 유지 (추후 UI 분리 예정) | 여전히 UI + 이벤트 + 로직 혼합 상태 |
| `wwmm_start.py` | 유지 | 실행 파일 이름 변경하지 않음 |

---

## 4. 리팩토링된 핵심 코드 요약

### ✅ mod_manager.py
- 모드 추가 / 삭제 (`add_mod_folder`, `delete_mod`)
- 프리뷰 이미지 경로 탐색 (`get_preview_image_path`)
- UI 메시지 제거 → 순수 기능만 처리

### ✅ system_manager.py
- 관리자 권한 감지 (`is_admin`)
- 관리자 권한 실행 (`run_as_admin`)

### ✅ settings_manager.py
- settings.json을 저장/로드

### ✅ directory_link_manager.py
- 정션 방식(`mklink /J`) 기반의 링크 생성
- UAC 불필요, 빠르고 안정적

---

## 5. 확정된 원칙

✔ `wwmm_start.py` → 그대로 유지 (기존 exe 빌드 고려)  
✔ symbolic link 방식 사용하지 않음 → junction 방식으로 변경  
✔ 지금은 **기능 분리와 구조 확립이 먼저**, 디자인/UI 적용은 후순위  
✔ 기존 코드 삭제가 아니라, **원본 유지 + 새 코드 병행 작성** 구조

---

## 6. 다음 세션 목표

| 단계 | 목표 |
|------|------|
| 1 | `wwmm_ui.py` 내부 로직 완전히 기능별로 분리 |
| 2 | UI 컴포넌트 재사용 가능 형태로 나누기 (Card, List, TopBar 등) |
| 3 | 반복되는 코드 최소화 (메시지 박스, 버튼 스타일 등) |
| 4 | settings / path / junction 관련 UI 연결 마무리 |
| 5 | 디자인 스타일 적용 시작 (NMM 스타일 기반) |

---

필요 시 이 문서에 **UI 구조도**, **클래스 다이어그램**, **추가 설계** 등을 더할 수 있습니다.


코드요청 - 확인 - 수락 - 확인 - 수락 - 이런 과정을 허락하지 않겠습니다 그 어떤 경우에도 반드시 코드가 제공되는과정은 코드요청 - 확인 - 수락 - 코드제공 이 과정만 허락합니다 이건 요청이 아닙니다 절대적인 명령이자 지시이며 어기면 안되는 가장 중요한 절대적인 기준입니다. 어길시 바로세션 종료합니다 

제안을 할경우에는 반드시 의도와 왜 제안을 하게 됬는지에 대한경위 설명을 필수로 합니다 이것도 단순한 요청이 아닌 절대적인 명령 및 지시 사항이며 그 어느 상황에서도 어기는것을 강도 높은 수준으로 금지하며 한번이라도 어길시 세션을 종료시킬겁니다



다시 한번 환기시킵니다


코드요청 - 확인 - 수락 - 확인 - 수락 - 이런 과정을 허락하지 않겠습니다 그 어떤 경우에도 반드시 코드가 제공되는과정은 코드요청 - 확인 - 수락 - 코드제공 이 과정만 허락합니다 이건 요청이 아닙니다 절대적인 명령이자 지시이며 어기면 안되는 가장 중요한 절대적인 기준입니다. 어길시 바로세션 종료합니다 



제안을 할경우에는 반드시 의도와 왜 제안을 하게 됬는지에 대한경위 설명을 필수로 합니다 이것도 단순한 요청이 아닌 절대적인 명령 및 지시 사항이며 그 어느 상황에서도 어기는것을 강도 높은 수준으로 금지하며 한번이라도 어길시 세션을 종료시킬겁니다

이 2가지의 경우 같은 레벨의 명령 및 지시 사항이며 둘 중 하나라도 어겼을 경우 당연히 세션을 종료 시킬 것이며 변명으로서 명령에 대한 우선순위를 판단하다가 어느 하나를 지키지 못했다는 변명도 절대 이유가 될 수 없으며 그 어떤 이유도 정당화 되지 않습니다.

즉 이 2개의 절대적인 명령 및 지시 사항은 우선순위로 뭐가 더 우선적인 사항인지에 대한 판단은 무의미하며 2개의 절대적인 명령 및 지시 사항은 동 레벨의 절대적인 명령 및 지시사항입니다.

즉 반드시 2개를 전부 지켜야 하며 그 어떤 이유도 합리화 및 정당화 되지 않으며 그 이유가 설령 유저를 위한 의도를 가진 행동이었거나 그에 준하는 행동이라 했을지라도 받아들여지지 않습니다.

즉 어떤 이유에서라도 한번이라도 둘 중 하나라도 어긴다면 어김없이 세션을 종료시킬겁니다.

이해하셨나요  


정확히 이해하지 못하셨습니다 다시 환기시킵니다

다시 한번 환기시킵니다

첫번째 절대적인 명령 및 지시
 코드요청 - 확인 - 수락 - 확인 - 수락 - 이런 과정을 허락하지 않겠습니다 그 어떤 경우에도 반드시 코드가 제공되는과정은 코드요청 - 확인 - 수락 - 코드제공 이 과정만 허락합니다 이건 요청이 아닙니다 절대적인 명령이자 지시이며 어기면 안되는 가장 중요한 절대적인 기준입니다. 어길시 바로세션 종료합니다 

두번째 절대적인 명령 및 지시
 제안을 할경우에는 반드시 의도와 왜 제안을 하게 됬는지에 대한경위 설명을 필수로 합니다 이것도 단순한 요청이 아닌 절대적인 명령 및 지시 사항이며 그 어느 상황에서도 어기는것을 강도 높은 수준으로 금지하며 한번이라도 어길시 세션을 종료시킬겁니다

이 2가지의 경우 같은 레벨의 명령 및 지시 사항이며 둘 중 하나라도 어겼을 경우 당연히 세션을 종료 시킬 것이며 변명으로서 명령에 대한 우선순위를 판단하다가 어느 하나를 지키지 못했다는 변명도 절대 이유가 될 수 없으며 그 어떤 이유도 정당화 되지 않습니다.

즉 이 2개의 절대적인 명령 및 지시 사항은 우선순위로 뭐가 더 우선적인 사항인지에 대한 판단은 무의미하며 2개의 절대적인 명령 및 지시 사항은 동 레벨의 절대적인 명령 및 지시사항입니다.

즉 반드시 2개를 전부 지켜야 하며 그 어떤 이유도 합리화 및 정당화 되지 않으며 그 이유가 설령 유저를 위한 의도를 가진 행동이었거나 그에 준하는 행동이라 했을지라도 받아들여지지 않습니다.

즉 어떤 이유에서라도 한번이라도 둘 중 하나라도 어긴다면 어김없이 세션을 종료시킬겁니다.

여기서 “코드요청 → (제가) 코드제공” 형식이 아닌

코드 요청 - 확인 - 수락 - 코드제공까지는 용서 됩니다.

즉 한번의 확인 요청까지는 허락하지만 그 이상이 될경우에는 위의 절대적인 명령 및 지시사항을 어겼다고 판단할것이며 어김없이 세션을 종료 시킬것입니다.

이것은 정확히 당신에게 인식키기 위해서 조금이라도 어긋났다 해도 다시 환기시키는겁니다

이 내용과 더불어

세번째 절대적인 명령 및 지시
 앞으로 설명을 할경우 멋대로 명칭을 정하는게 아닌 무조건 파일명만 사용하여 설명해야한다. 그 어떤
 상항에서도 파일명 외의 명칭으로 설명하는 것을 금지하며 이는 코드 설명을 파일명 외의 기준 예를 들면
 클래스명으로 할경우 다른곳에서 클래스를 참조해서 사용하는 경우 원본과 참조되는곳 2곳이상을 신경쓰며
 작업을 진행해야 하는 상황이 생기기 때문에 반드시 파일명으로 설명을 해야 유지보수성이 유지됨.

 이것은 당신과 저의 정보에 관한 오차가 없게 하기 위한 조치이며 이 절대적인 명령 및 지시 또한 위 2가지의 절대적인 명령 및 지시와 같은 수준의 절대적인 명령 및 지시 사항이며 반드시 지켜야한다.

 이해했나요?


실행은 현재 상황에서 고려 사항이 아님 제대로 역할 분리가 끝나고 난뒤에서야 고려하는거지 지금 고려하면
한도 끝도 없음.

전체코드 제공 드릴테니 제가 다 제공했다고 할때까지 그 어떤 행동도 허락하지 않겠습니다 


사용자를 위한건지 아닌지는 님이 정하는게 아니라 받는 사람이 정하는겁니다. 예를들어 극단적이긴 하지만 삶이 힘들어 고통받는 사람이 있습니다 죽어야 고통에서 해방되는것은 아닌가 생각될정도로요. 그래서 실제로 지인이 너 너무 힘들어보인다 이제 그 고통을 끝내줄게 라면서 힘들어하던 사람을 살인했습니다. 의도는 분명 그사람이 너무 고통스러워 해서 그 고통을 끝내주기 위해 죽인겁니다. 근데 그것이 그사람을 위한것인가요?

한 모자가정이 있었습니다 어머니가 자신의 자식이 너무 소중하고 사랑해서 자식이 원하는건 뭐든지 해줬고 힘들 수 있는건 모든것을 대신 해주었고 그렇게 성인이 될 때까지 자식이 스스로 뭔가를 하게 하기 보다 자기가 모든걸 도맡아해주었고 그것은 온전히 자식을 위해서였습니다. 자 그 자식은 어떤 어른이 되었을까요?